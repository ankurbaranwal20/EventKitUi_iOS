# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Crashlytics Demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Crashlytics Demo
   pod 'Firebase/Analytics'
   pod 'Fabric', '~> 1.10.2'
   pod 'Crashlytics', '~> 3.14.0'
  target 'Crashlytics DemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Crashlytics DemoUITests' do
    # Pods for testing
  end

end
